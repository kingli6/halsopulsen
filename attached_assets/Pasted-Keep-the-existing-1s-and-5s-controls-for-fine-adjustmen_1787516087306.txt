Keep the existing ±1s and ±5s controls for fine adjustments.

However, make the displayed duration itself directly editable.

For example:

3:00

When the user taps/clicks it, allow them to enter a new duration such as:

10:00

Requirements:

Support direct editing of the duration.
Accept a simple MM:SS format.
Validate the input.
Prevent invalid/negative durations.
Preserve the existing ±1s and ±5s controls.
After editing, the new duration should immediately be reflected in the section.
Avoid requiring dozens of button presses to make a large change.

Please use the existing timer/state architecture rather than creating a second independent timer system.

2. Massage section card layout

Improve the section cards without changing their overall visual style unnecessarily.

Remove the body-part emoji/icon if the existing Massage preset currently displays one, for example:

💪 Right arm

Use:

Right arm

Fix the × remove button so it is always contained comfortably inside the card.

Use a layout with a dedicated/fixed area for the remove button so the duration controls can never push the × outside the card, especially on mobile.

The card should remain usable on narrow screens.

3. Running timer navigation

Use a YouTube-style back behavior rather than adding a separate "Börja om" button.

The back button should be an explicit timer navigation control — not browser history and it must not navigate away from the workout page.

Label it:

← Föregående

But give it the following behavior:

First press while partway through the current section

Restart the current section from its full duration.

Example:

Right thigh — 2:34 remaining

Press:

← Föregående

Result:

Right thigh — 5:00

Second press shortly afterward

If the user presses the back button again within approximately 2 seconds, move to the previous section.

Example:

Right thigh — 5:00

Press back again quickly:

Left arm — 3:00

If the current section is already at its beginning

If the current section has just started and is essentially at its full duration, pressing back should immediately move to the previous section.

This should make the behavior feel similar to familiar media-player navigation:

First back = restart current section.
Quick second back = previous section.
Back at the beginning = previous section.

Do not use browser history.back().

The timer should remain on the workout page.