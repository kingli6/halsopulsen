Task: Add dynamic total time, running-time progress, and drag-and-drop section ordering

Please build on the existing Massage timer implementation. Do not rebuild the timer or change unrelated presets.

The Massage preset is now an editable sequence of sections. I want it to behave more like a playlist: I should be able to see the total duration, modify individual sections, add/remove sections, and freely reorder them.

Implement the following changes.

1. Change the Massage preset title

The current Quick Select / Snabbval title is:

Massage - 46 min

Change this to simply:

Massage

Do not include the duration in the preset's static title.

The total duration is dynamic and must be displayed separately.

For example:

Massage

46:00 total

If the user changes the sequence to 52 minutes, the title should still be:

Massage

not:

Massage - 52 min

The duration should always come from the actual current section configuration.

2. Show total sequence duration in the Massage setup screen

When creating/editing the Massage sequence, display the sum of all section durations prominently.

For example:

Massage

Total time
46:00

1. Right arm       3:00
2. Left arm        3:00
3. Right thigh     5:00
4. Left thigh      5:00
5. Right glute     4:00
6. Left glute      4:00
7. Back           15:00
8. Neck & shoulders 7:00

The total must be calculated dynamically from the current sections.

Examples

If the original total is:

46:00

and the user changes Right arm:

3:00 → 10:00

the total should immediately become:

53:00

If the user removes a:

3:00

section:

46:00 → 43:00

If the user adds a new:

2:00

section:

46:00 → 48:00

If the user changes a section from:

5:00 → 7:30

the total should immediately increase by 2:30.

Do not hard-code the total.

Use the actual current section configuration as the source of truth.

3. Show total time clearly while editing

The total duration should be visually distinct from the individual sections.

For example:

Massage

TOTAL
46:00

────────────────────

Right arm
3:00

Left arm
3:00

...

It doesn't need to be huge, but it should be easy to see at a glance.

The total should update immediately whenever:

A duration is edited
+1s is pressed
-1s is pressed
+5s is pressed
-5s is pressed
A section is added
A section is removed
Sections are reordered

Reordering itself doesn't change the total, but the total should remain synchronized with the current sequence.

4. Show total duration on the running timer

When the Massage sequence starts, I want to know not only the current section's remaining time but also how much time remains in the entire sequence.

For example:

RIGHT ARM

2:14

Next
Left arm · 3:00


Total remaining
42:14

← Föregående      Hoppa över

             PAUSA

                 Avbryt

The exact visual layout can follow the existing design, but the information should be clearly available.

Important distinction

There are two timers:

Current section

Example:

2:14

Entire sequence

Example:

42:14 remaining

The sequence timer must account for:

Remaining time in the current section
All subsequent sections
Subsequent rounds/blocks if the existing timer supports rounds

It must NOT simply display the original total duration after the timer starts.

5. Make the total-time display clickable

Use a simple YouTube-like interaction.

The total-time display should be clickable/tappable.

For example, while running:

42:14 remaining

Tap it:

3:46 elapsed